import { HuskyShopValidators } from './husky-shop-validators';

describe('HuskyShopValidators', () => {
  it('should create an instance', () => {
    expect(new HuskyShopValidators()).toBeTruthy();
  });
});
